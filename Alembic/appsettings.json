{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    },
    "Console": {
      "FormatterOptions": {
        "IncludeScopes": true
      }
    }
  },
  "AllowedHosts": "*",

  "//": [
    "The section is 'Morgana:LLM' and not 'Alembic:LLM' because MorganaLLM reads that path:",
    "Alembic consumes the framework's own provider abstraction rather than reimplementing it.",
    "Only the Performance tier is declared — Alembic writes dispositive prose and runs once per",
    "onboarding, so it never uses the Efficiency die. A single-tier deployment therefore does",
    "not serve Alembic until a Performance entry exists (no cross-tier fallback, by design).",
    "MagicDust pricing is declared because TierDefinition requires it, not because Alembic",
    "meters anything: there is no conversation here to charge a budget to.",
    "",
    "MaxOutputTokens is generous because Alembic emits whole C# source files, not turns of prose,",
    "and because a reasoning model spends the budget on thinking FIRST: a cap that bites yields a",
    "response of zero characters rather than a short one. Removing the field entirely was tried and",
    "is worse — Anthropic requires max_tokens, so the SDK default applies and it is smaller still.",
    "This is the only place the number belongs: nothing in Alembic's code declares one, and",
    "ToolMockService streams and resumes until the model stops of its own accord, so a file longer",
    "than whatever is configured here still completes.",
    "",
    "Declaring Efficiency here as well was tried and is wrong. This is the same section a Morgana",
    "instance configures, so a shared store (in-repo, the user secrets Alembic borrows from",
    "Morgana.Web) does set an Efficiency ModelId, which then binds against no MagicDust and yields",
    "a half-populated tier. That costs one chat client Alembic never asks for and nothing else,",
    "since GetPricing is never called for it. Declaring Efficiency instead makes the standalone",
    "case fail hard: an operator who configures only the tier Alembic uses leaves the other on its",
    "placeholder, and MorganaLLM.RegisterTierClient refuses to start. Harmless beats fatal."
  ],
  "Morgana": {
    "LLM": {
      "Provider": "_FUNCTIONAL_OVERRIDE_",
      "Anthropic": {
        "ApiKey": "_SECURE_OVERRIDE_",
        "Tiers": {
          "Performance": {
            "Options": {
              "ModelId": "_FUNCTIONAL_OVERRIDE_",
              "MaxOutputTokens": 16384
            },
            "MagicDust": {
              "InputTokensPerDustUnit": 5333,
              "OutputTokensPerDustUnit": 1067,
              "CachedInputWeight": 0.10,
              "CacheCreationWeight": 2.00
            }
          }
        }
      },
      "AzureOpenAI": {
        "ApiKey": "_SECURE_OVERRIDE_",
        "Endpoint": "_SECURE_OVERRIDE_",
        "Tiers": {
          "Performance": {
            "Options": {
              "ModelId": "_FUNCTIONAL_OVERRIDE_",
              "MaxOutputTokens": 16384
            },
            "MagicDust": {
              "InputTokensPerDustUnit": 3072,
              "OutputTokensPerDustUnit": 768,
              "CachedInputWeight": 0.50,
              "CacheCreationWeight": 1.00
            }
          }
        }
      },
      "OpenAI": {
        "ApiKey": "_SECURE_OVERRIDE_",
        "Tiers": {
          "Performance": {
            "Options": {
              "ModelId": "_FUNCTIONAL_OVERRIDE_",
              "MaxOutputTokens": 16384
            },
            "MagicDust": {
              "InputTokensPerDustUnit": 3072,
              "OutputTokensPerDustUnit": 768,
              "CachedInputWeight": 0.50,
              "CacheCreationWeight": 1.00
            }
          }
        }
      },
      "Ollama": {
        "Endpoint": "_SECURE_OVERRIDE_",
        "Tiers": {
          "Performance": {
            "Options": {
              "ModelId": "_FUNCTIONAL_OVERRIDE_",
              "MaxOutputTokens": 16384
            },
            "MagicDust": {
              "InputTokensPerDustUnit": 0,
              "OutputTokensPerDustUnit": 0,
              "CachedInputWeight": 1.00,
              "CacheCreationWeight": 1.00
            }
          }
        }
      }
    }
  }
}