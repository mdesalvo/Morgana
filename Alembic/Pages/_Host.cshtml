@page "/"
@namespace Alembic.Pages
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <base href="~/" />

        <title>Alembic - Morgana's Authoring Workbench</title>

        <link href="css/site.css" rel="stylesheet" asp-append-version="true" />
    </head>
    <body>
        <!--
            Blazor Server root component: UI state lives on the server, DOM diffs travel over SignalR.
            ServerPrerendered rather than Server: the first paint carries real content instead of an
            empty shell. It also means the page is inspectable without a browser, which is how the
            scaffold proves the prompt stack resolves.
        -->
        <component type="typeof(App)" render-mode="ServerPrerendered" />

        <div id="blazor-error-ui" style="display: none;">
            An error has occurred.
            <a href="" class="reload">Reload</a>
            <a class="dismiss">🗙</a>
        </div>

        <script src="_framework/blazor.server.js"></script>
        <script>
            // The transcript is a fixed-height scroll box, so without this every new turn — the
            // client's own answer, Alembic's "thinking", the reply — lands below the fold and
            // pressing a button looks like nothing happening.
            window.alembicScrollTranscript = () => {
                const box = document.querySelector('.transcript');
                if (box) box.scrollTop = box.scrollHeight;
            };
        </script>
    </body>
</html>
