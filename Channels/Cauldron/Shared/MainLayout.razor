@inherits LayoutComponentBase

<div class="page">
    <main>
        @Body
    </main>
</div>

@*
    Minimal layout: the chat fills the viewport, so there is no header, nav, sidebar or footer.
    The .page and main wrappers exist purely as CSS hooks.
*@
